# ADR-`<NNNN>`: `<Short decision title, in the imperative>`

Template for a lightweight Architecture Decision Record; copy to `SPEC/decisions/<NNNN>-<kebab-title>.md`, using the next free number.

<!-- Delete every instruction comment before committing. One page maximum. -->

| | |
|---|---|
| Status | `<Proposed / Accepted / Superseded by ADR-NNNN / Deprecated>` |
| Date | `<YYYY-MM-DD>` |
| Deciders | `<who>` |
| Supersedes | `<ADR-NNNN, or none>` |

## Context

<!-- The forces at play: the constraint, the problem, what was already true. Written so someone with no memory of the conversation understands why a decision was even needed. No solution here. -->

`<context>`

## Decision

<!-- One or two sentences, active voice: "We will …". Be specific enough to be falsifiable. -->

We will `<decision>`.

## Consequences

| | |
|---|---|
| Positive | `<what this makes easy or cheap>` |
| Negative | `<what this makes hard, and the cost accepted>` |
| Follow-up needed | `<work this creates, or none>` |

## Alternatives considered

| Alternative | Why not |
|---|---|
| `<option>` | `<reason rejected — the real reason, not a strawman>` |
